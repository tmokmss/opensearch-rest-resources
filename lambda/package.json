{
    "name": "lambda",
    "version": "1.0.0",
    "description": "",
    "type": "module",
    "main": "index.js",
    "scripts": {
        "build": "esbuild index.ts --bundle --minify --outdir=./dist --platform=node --external:@aws-sdk/*"
    },
    "devDependencies": {
        "@aws-sdk/client-secrets-manager": "^3.535.0",
        "@types/aws-lambda": "^8.10.95",
        "@types/node": "^18.13.0",
        "esbuild": "^0.14.39"
    }
}
